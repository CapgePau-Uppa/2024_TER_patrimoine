version: '2'
services:
  db:
    image: mysql:latest
    volumes:
      - ./db/container:/docker-entrypoint-initdb.d
    environment:
      - MYSQL_DATABASE=ter_bd
      - MYSQL_ROOT_PASSWORD=root
    ports:
      - "3306:3306"
    expose:
      - "3306"